# Week 11 — Roofline 拐点 + 量化（上）

**Dates**: 2026-11-08 → 2026-11-14  
**Goal**: 把"decode 卡在带宽上"这条主线收口——先实测 roofline 脊点在哪，再看量化能把它推到哪。

**Time budget**: 4-6 小时 ｜ **GPU 成本**: ~$2

---

## 为什么这两件事放同一周

Wk 8-10 用三种方式验证了同一件事：**decode 时间 ∝ 每 token 要搬的权重字节数**。

| 周 | 手段 | 结果 |
|---|------|------|
| 8 | 投机解码白嫖空闲算力 | batch=1 有效，compute-bound 后失效 |
| 9 | TP 把每卡权重减半 | 1.55x（不是 2x，差额是通信） |
| 10 | MoE 只读激活权重 | 7B vs 14B 近线性（97.2 vs 54.5） |

这周补上最后两块：
- **提高 batch** —— 不减少搬运，而是让每次搬运服务更多 token（提高算术强度）
- **量化** —— 直接减少要搬的字节数，且**不需要加卡**

两者动的是同一个分式的分子和分母，所以放一起对照。

---

## Task 1 — Roofline 脊点实测（延后到本周，Wk10 讨论时提出）

### 理论预测

A100：算力 312 TFLOPS（fp16 tensor core），带宽 2,039 GB/s。

```
脊点 = 312e12 / 2039e9 ≈ 153 FLOP/byte
```

对权重固定型 GEMM `[M×K] @ [K×N]`：

```
FLOPs   = 2·M·K·N
读字节   = 2·K·N          （权重，fp16）
算术强度 = 2MKN / 2KN = M   ← 恰好等于 batch
```

**所以算术强度就是 M。** 预测：

| batch | 算术强度 | 距脊点 |
|-------|---------|-------|
| 32 | 32 | 21% —— 仍是带宽瓶颈，应保持近线性 |
| 128 | 128 | 84% —— 开始偏离线性 |
| 153 | 153 | 100% —— 转为算力瓶颈 |
| 256+ | 256+ | 应明显饱和 |

### 实验

- [ ] batch = 1 / 8 / 32 / 64 / 128 / 256 / 512，Qwen2.5-7B 单卡
- [ ] 画 batch × throughput，找偏离线性的拐点
- [ ] 对照预测的 M≈153

**为什么扫到 512 就停**：512 是 vLLM 的 `max_cudagraph_capture_size`（日志里那串
capture sizes 的最后一个）。超过它会退回 eager 模式，**变成两个变量同时变**，
拐点就说不清是 roofline 还是 CUDA graph 造成的。停在 512 保证只有一个自变量。

---

## Task 1.5 — CUDA graph 的开销占比（同一组 batch，加跑一遍 eager）

**背景**：一次 decode 要 launch 约 300-400 个 kernel（每层十几个 × 28 层），
每次 launch 约 5-10 µs 的 CPU 开销 → **约 2ms/token 的纯调度开销**。
TPOT 才 10.3ms，所以这可能占了五分之一。

CUDA graph 把整串 launch 录成一张 DAG，之后一次调用整体重放，CPU 开销近乎归零。
代价是形状写死（所以要预录离散尺寸 + 向上取整）和额外显存（日志里 0.53 GiB）。

**假设**：launch 开销是**每步固定**的，而每步计算量随 batch 增长
→ **CUDA graph 的收益应该随 batch 单调递减**，和 roofline 是同一根轴。

- [ ] 同样的 batch 扫描，加跑 `enforce_eager=True`
- [ ] 算每档的 graph 收益 = `default / eager`

**顺带补一个欠账**：Wk 3 测到 vLLM 单请求是 HF 的 2x，当时归因为
"CUDA graph + torch.compile"，但**从未实测**。这个实验能把那 2x 拆开。

```python
LLM(model=..., enforce_eager=True)   # 关掉 CUDA graph
```

**显存不成问题**：Wk 9 测到 KV cache 容量 1,052,560 tokens，
每条请求约 271 tokens（15 prompt + 256 output）→ 理论可容纳约 3,870 条并发。

**要回答的**：拐点是否落在 M≈150 附近？如果明显更早，说明还有别的瓶颈
（调度开销、CUDA graph 的 capture size 上限是 512、采样开销）。

---

## Task 2 — INT8 / FP8 量化

- [ ] FP8 权重量化（A100 是 sm80，**不支持原生 FP8 计算**，只能做存储量化）
- [ ] INT8（W8A8）
- [ ] 对比 tok/s、TPOT、显存占用
- [ ] 抽查输出质量是否退化

**预期**：权重体积减半 → batch=1 的 decode 速度应接近翻倍，
因为那里纯粹是带宽瓶颈。**大 batch 下收益应该小得多**——已经靠近算力瓶颈了。

这个预期正好和 Task 1 的曲线互相印证：**量化的收益应该在脊点左侧最大，右侧趋近于零。**

---

## Task 3 — 日志

- [ ] 更新 `progress.md`
- [ ] commit + push

---

## What "done" for Wk 11 looks like

1. 有 batch 1→512 的完整曲线，能指出拐点
2. 有 fp16 vs INT8 的对比，且能解释为什么收益随 batch 衰减
3. 能把"量化 / 加 batch / TP / MoE"四种手段统一到 roofline 图上

---

## 结果（跑完后填）

### Task 1 — batch sweep

| batch | tok/s | vs 线性外推 | 算术强度 | eager tok/s | graph 收益 |
|-------|-------|-----------|---------|------------|-----------|
| 1 | | 1.00x | 1 | | |
| 8 | | | 8 | | |
| 32 | | | 32 | | |
| 64 | | | 64 | | |
| 128 | | | 128 | | |
| 256 | | | 256 | | |
| 512 | | | 512 | | |

### Task 2 — 量化

| 精度 | 权重体积 | batch=1 | batch=32 | batch=256 |
|------|---------|---------|----------|-----------|
| fp16 | 14.2 GiB | | | |
| INT8 | | | | |
