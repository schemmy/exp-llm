# Week 12 — INT4 量化 + Tradeoff 总表

**Dates**: 2026-11-15 → 2026-11-21  
**Goal**: 测 INT4 的速度/质量权衡，同时回答 Wk 11 留下的一个悬念——
INT8 几乎零加速，是量化格式本身的问题，还是那条 kernel 路径没优化好？

**Time budget**: 3-4 小时 ｜ **GPU 成本**: ~$2

---

## 为什么这周能验证 Wk 11 的悬念

Wk 11 的结论：`int8_per_channel_weight_only` 全程 ~1.0x，**没有兑现"减字节=加速"的承诺**。
猜测是它走的是通用 dequant kernel，没有专门优化。

**这周换一条完全不同的路**：不用 vLLM 的"加载时动态量化"，改用**预量化 checkpoint**
（Qwen 官方发布的 AWQ / GPTQ-Int4 版本），并显式指定 `awq_marlin` / `gptq_marlin`——
这是 vLLM 里公认优化最好的量化 kernel 之一（Marlin，专为 Ampere+ 设计的 INT4×fp16 GEMM）。

**如果 INT4 这次真的兑现了加速**，就说明 Wk 11 INT8 的问题确实出在 kernel 上，
不是"量化本身注定没用"。**如果 INT4 也不加速**，那说明 A100 上做权重量化这件事
本身就有更根本的限制，值得重新审视整个假设。

---

## 权重体积这次是真的——和 Wk 11 不同

Wk 11 的 fp8/int8 都是**加载时动态量化**：下载下来的 checkpoint 本来就是 fp16，
量化发生在读进显存之后，所以"权重体积"那一列测的是下载体积，没有意义。

这周用的是 Qwen 官方**预量化**的 checkpoint——磁盘上的文件本来就是 4-bit。
`weight_gib` 这次测的是真实的量化后体积，Wk 11 的那个测量局限本周不存在。

---

## Task 1 — AWQ vs GPTQ-Int4 vs fp16，batch sweep

- [x] 跑 `experiments/wk12_int4/int4_quant.py`
- [x] 模型：`Qwen/Qwen2.5-7B-Instruct-AWQ`（`quantization="awq_marlin"`）
      和 `Qwen/Qwen2.5-7B-Instruct-GPTQ-Int4`（`quantization="gptq_marlin"`）
- [x] batch = 1 / 32 / 256（和 Wk 11 完全对齐，直接可比）

**预期**：如果 Marlin kernel 确实高效，batch=1 应该比 fp16 快不少
（4-bit 存储只有 fp16 的 1/4，理论上限比 FP8 的 2x 更高）。
batch=256 的算力区应该重演 FP8 那种"净亏"——甚至可能亏得更多，
因为 4-bit 反量化到 fp16 的开销比 8-bit 更大。

---

## Task 2 — 质量抽查：5 个固定 prompt，相似度对比 fp16

- [x] 同样的 5 个问题，greedy 解码，AWQ / GPTQ / fp16 各生成一次
- [x] 用字符级相似度（`difflib.SequenceMatcher`）粗略量化"输出偏离 fp16 多少"
- [x] 肉眼过一遍，看有没有真正的退化——唯一贴出的完整样本正常，
      但低分 prompt（0.08/0.12）没拿到完整文本，留作待查

这不是严格的困惑度评测，只是比 Wk 11 那种单个 prompt 肉看更systematic 一点的信号。

---

## Task 3 — 汇总 Phase 1 的完整量化 tradeoff 表

把 Wk 11 + Wk 12 的数字拼成一张总表：fp16 / FP8 / INT8 / AWQ-INT4 / GPTQ-INT4，
每行标注权重体积、三档 batch 的吞吐、相对 fp16 的加速比、质量相似度。

这张表是 Phase 1 quantization 部分唯一的交付物，也是 Blog #1 会直接引用的素材。

---

## Task 4 — 日志

- [x] 更新 `progress.md`
- [x] commit + push

---

## What "done" for Wk 12 looks like

1. 有 AWQ 和 GPTQ-Int4 的完整 batch sweep 数字
2. 回答了 Wk 11 的悬念：INT8 零加速是 kernel 问题还是格式问题
3. 有一张跨 5 种精度的完整 tradeoff 表，可以直接拿去写 Blog #1

---

## 结果（跑完后填）

### Task 1 — 吞吐

| 精度 | 权重体积 | batch=1 | batch=32 | batch=256 |
|------|---------|---------|----------|-----------|
| fp16（参照，Wk 11） | 14.19 GiB | 93.8 | 2,835.1 | 10,659.4 |
| **AWQ-INT4** | 5.19 GiB | 206.3 | 5,003.7 | 9,861.4 |
| **GPTQ-INT4** | 5.19 GiB | 208.9 | 4,906.1 | 10,332.9 |

加速比（相对 fp16）：

| 精度 | batch=1 | batch=32 | batch=256 |
|------|---------|----------|-----------|
| AWQ-INT4 | **2.20x** | **1.76x** | 0.93x |
| GPTQ-INT4 | **2.23x** | **1.73x** | 0.97x |

---

## 悬念解开：Wk 11 INT8 零加速是 kernel 问题，不是格式问题

同样是"整数量化"，Wk 11 的 `int8_per_channel_weight_only`（无优化 kernel）全程 ~1.0x；
这周的 INT4（走 `awq_marlin` / `gptq_marlin`）在 batch=1 达到 **2.2x** ——
比存储量更大的 FP8（1.56x）还快。**同一个"整数量化"概念，kernel 实现的差距
能拉开一个数量级的效果差异。** Marlin 把解压和矩阵乘融合在一个 kernel 里，
避免了"先解压写回显存、再读出来做矩阵乘"这多绕的一圈——这正是这周开头
week12.md 计划里提出的那个问题的答案。

---

## 预测错了的地方：INT4 在算力区反而比 FP8 亏得少

**原预测**：4-bit 反量化开销比 8-bit 更大，所以 batch=256 应该比 FP8 的 0.84x 亏得更狠。

**实测相反**：AWQ/GPTQ 在 batch=256 分别是 0.93x / 0.97x，**比 FP8 亏得更少**。

合理的推测（没有直接证据，推测不是确认的机制）：Marlin 对 4-bit 的融合 kernel
写得足够高效，反量化开销在整个 kernel 时间里占比很小；而且真实硬件的
"算力瓶颈"不是非黑即白的开关——字节数更小，哪怕在算力区也还能省下一点点，
只是不再是主导因素。这和 Wk 9 nvidia-smi topo 失败一样，留作未解问题，
不硬拗成确定结论。

---

## 追问：INT4 比 FP8 快，是 kernel 更强，还是起跑线更靠前？

算一下理论加速上限（fp16 体积 / 量化后体积），再看实测数字兑现了这个上限的百分之多少：

| 精度 | 理论上限 | 实测（batch=1） | 兑现率 |
|------|---------|----------------|--------|
| INT4 | 14.19/5.19 = **2.73x** | 2.20x | **80.6%** |
| FP8 | 14.19/7.10 = **2.00x**（未排除任何层） | 1.56x | **78.0%** |

**两者兑现率几乎一样**（78-81%），说明 Marlin（INT4）和 vLLM 的 FP8 kernel
在"把理论带宽收益翻译成实际速度"这件事上是同一个量级的效率，
不存在谁的 kernel 天生更强。**INT4 赢在起跑线更靠前**——它的体积天花板
本来就比 FP8 高，兑现比例相近的情况下，起点高终点自然也高。

顺带纠正一个容易有的直觉：INT4 变快**不是因为"算得更简单"**。
A100（sm80）没有原生 INT4 tensor core，Marlin 解压回 fp16 之后走的
是和 fp16 基线完全一样的 tensor core，乘加次数（FLOPs）没有变。
变小的只是要从显存搬进来的字节数——量化优化的始终是 MBU，不是 MFU。

---

## AWQ 和 GPTQ：速度几乎一样，符合预期

206.3 vs 208.9、5,003.7 vs 4,906.1、9,861.4 vs 10,332.9——三个点差距都在个位百分比。
**符合预期**：两者走的是同一个 Marlin kernel，算法差异（怎么挑量化数值）
不该体现在速度上，只该体现在质量上。这正是 Wk 11 那次量化闲聊里讲的
"三层分离"模型——AWQ/GPTQ 决定数值，kernel 决定速度——的直接印证。

---

## 权重体积：36.6%，不是理论的 25%

4-bit 理论上应该是 fp16 的 1/4（14.19/4 ≈ 3.55 GiB），实测是 **5.19 GiB（36.6%）**。

推测（未直接验证）：AWQ/GPTQ 的常见做法是把 embedding 和 lm_head 层留在 fp16——
这两层是查表操作，量化性价比低。Qwen2.5-7B 词表 15 万+，这两层加起来
大概十亿参数量级，留 fp16 正好能解释多出来的约 1.6 GiB 缺口。
没有直接看 checkpoint 的 `config.json` 确认，是数量级匹配的推算。

### Task 2 — 质量相似度

| 精度 | 平均相似度 vs fp16 | 各 prompt 相似度 |
|------|-------------------|------------------|
| AWQ-INT4 | 0.30 | 0.59 / 0.39 / 0.12 / 0.26 / 0.12 |
| GPTQ-INT4 | 0.31 | 0.69 / 0.27 / 0.08 / 0.46 / 0.08 |

**这个数字有陷阱，不能直接当"质量差"读。** `difflib` 是逐字符比对整段文本，
只要开头几个词措辞不一样（量化后权重数值变了，贪心解码合法地走上不同路径），
后面整段哪怕语义完全正确也会被判定"不相似"——这和 Wk 11 FP8 遇到的情况一样。

唯一贴出完整文本的那条（hash table 定义）：

> **[fp16]** "A hash table is a data structure that **stores key-value pairs and uses**
> a hash function to compute an index into an array of buckets or slots..."
> **[gptq_int4]** "A hash table is a data structure that **maps keys to values using**
> a hash function to compute an index into an array of buckets or slots..."

**完全正确，只是换了个说法**——不是退化。但 0.08、0.12 这几个低分 prompt
没有拿到完整文本（脚本只打印了第一条样本），**这是本周唯一的遗留项**：
应该单独把这几条拉出来确认不是真正的复读/跑题，而不只是"措辞不同"。

### Task 3 — Phase 1 量化总表

| 精度 | 权重体积 | batch=1 加速 | batch=32 加速 | batch=256 加速 | 质量相似度 |
|------|---------|-------------|--------------|---------------|-----------|
| fp16 | 14.19 GiB | 1.00x | 1.00x | 1.00x | — |
| FP8 | 14.19 GiB* | 1.56x | 1.51x | 0.84x | — |
| INT8 | 14.19 GiB* | 1.03x | 1.03x | 1.06x | — |
| **AWQ-INT4** | **5.19 GiB (36.6%)** | **2.20x** | **1.76x** | 0.93x | 0.30 |
| **GPTQ-INT4** | **5.19 GiB (36.6%)** | **2.23x** | **1.73x** | 0.97x | 0.31 |

*FP8/INT8 的权重体积是 Wk 11 遗留的测量局限（测的是下载体积不是量化后体积，
因为那两种是加载时动态量化）。AWQ/GPTQ 是真正的预量化 checkpoint，5.19 GiB 是实数。

**这张表最该带走的结论**：五种精度里，**INT4（走 Marlin）在带宽区（batch 1-32）
吞吐最高，同时权重体积最小**——速度和体积双赢，没有中间地带的取舍。
唯一的代价在算力区（batch=256）,所有量化方案在那里都或多或少吃亏,
INT4 反而是里面亏得最少的一个。

**生产判断**：如果服务的流量落在带宽区（小 batch、延迟敏感），INT4-Marlin
是这张表里最优的选择，没有争议。如果流量稳定在大 batch（算力区），
量化本身的价值要打个问号——fp16 在那个区间已经接近它能达到的最好效率，
这时候该考虑的是别的手段（TP、MoE、更大的 batch 窗口)而不是量化。
